# robot_firmware

Arduino Mega 2560 sketches for low-level motor control and encoder tick streaming.

Primary sketch: `robot_firmware.ino` (Phase 1 drive + Phase 2 odometry tick stream over USB serial).

This directory is firmware source only - not a ROS 2 package.
